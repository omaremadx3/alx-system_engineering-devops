#!/bin/bash

# Prompt for the file name
read -p "Enter the name of the file: " filename

# Create the file with the shebang line
echo "#!/bin/bash" > "$filename"

echo "File '$filename' created with the shebang line."

